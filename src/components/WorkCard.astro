---
import type { WorkItem } from '../data/work';

interface Props {
  item: WorkItem;
  /** 'compact' = Home's 3-up grid. 'full' = Work index: 16:9, bigger type, tags. */
  variant?: 'compact' | 'full';
}
const { item, variant = 'compact' } = Astro.props;
const showTags = variant === 'full';
---

<a class:list={['rth-card', 'work-card', variant]} href={item.href}>
  <div class="thumb">
    {item.image ? (
      <img src={item.image} alt={`${item.title} — screenshot`} loading="lazy" decoding="async" />
    ) : (
      <div class="thumb-fallback" role="img" aria-label={`${item.title} (${item.tagline})`}>
        <div class="fallback-title">{item.title}</div>
        <div class="fallback-meta">{item.tagline}</div>
      </div>
    )}
  </div>
  <div class="body">
    <div class="title">{item.title}</div>
    <div class="tagline">{item.tagline}</div>
    <p class="summary">{item.summary}</p>
    {
      showTags && (
        <div class="tags">
          {item.tags.map((tag) => (
            <span class="rth-chip">{tag}</span>
          ))}
        </div>
      )
    }
  </div>
</a>

<style>
  .work-card {
    display: flex;
    flex-direction: column;
    text-decoration: none;
    overflow: hidden;
  }

  .thumb {
    width: 100%;
    aspect-ratio: 16 / 10;
    overflow: hidden;
    background: var(--surface-card-alt);
  }
  .thumb img {
    width: 100%;
    height: 100%;
    object-fit: cover;
  }
  .thumb-fallback {
    width: 100%;
    height: 100%;
    display: flex;
    flex-direction: column;
    justify-content: flex-end;
    padding: 16px;
    background:
      linear-gradient(140deg, rgba(92, 193, 255, 0.08), rgba(143, 0, 255, 0.08)),
      var(--surface-card-alt);
  }
  .fallback-title {
    font-family: var(--font-ui);
    font-size: 16px;
    font-weight: 600;
    color: var(--text-primary);
    line-height: 1.3;
  }
  .fallback-meta {
    margin-top: 6px;
    font-size: 12.5px;
    line-height: 1.45;
    color: var(--text-faint);
  }

  .body {
    padding: 20px 22px 24px;
  }

  .title {
    font-family: var(--font-ui);
    font-size: 18px;
    font-weight: 600;
    color: var(--text-primary);
    margin-bottom: 6px;
  }

  .tagline {
    font-family: var(--font-body);
    font-size: 13.5px;
    color: var(--text-meta);
    font-weight: 500;
    margin-bottom: 10px;
  }

  .summary {
    font-family: var(--font-body);
    font-size: 14px;
    line-height: 1.6;
    color: var(--text-muted);
    margin: 0;
  }

  .tags {
    display: flex;
    flex-wrap: wrap;
    gap: 8px;
    margin-top: 16px;
  }

  /* Work index variant — larger thumb and type than Home's 3-up grid. */
  .full .thumb {
    aspect-ratio: 16 / 9;
  }
  .full .body {
    padding: 24px 26px 28px;
  }
  .full .title {
    font-size: 20px;
  }
  .full .tagline {
    margin-bottom: 12px;
  }
  .full .summary {
    font-size: 14.5px;
    line-height: 1.65;
  }
</style>
